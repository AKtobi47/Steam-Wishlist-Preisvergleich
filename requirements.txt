# requirements.txt - Steam Price Tracker (KORRIGIERT)
# Nur externe Dependencies - eingebaute Module entfernt

# ========================================
# KERN-REQUIREMENTS (ZWINGEND ERFORDERLICH)
# ========================================

requests>=2.31.0
schedule>=1.2.0
python-dotenv>=1.0.0
colorlog>=6.7.0
tqdm>=4.66.0
python-dateutil>=2.8.0

# ========================================
# OPTIONAL: DATENANALYSE & VISUALISIERUNG
# ========================================

pandas>=2.1.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0
numpy>=1.24.0
scipy>=1.11.0

# ========================================
# ENTFERNTE EINGEBAUTE MODULE
# ========================================
# 
# Diese Module sind in Python eingebaut und dürfen NICHT in requirements.txt:
# - tkinter (GUI - eingebaut in Python)
# - argparse (CLI parsing - eingebaut)
# - zipfile (ZIP-Verarbeitung - eingebaut)
# - csv (CSV-Verarbeitung - eingebaut)
# - json (JSON-Verarbeitung - eingebaut)
# - sqlite3 (Datenbank - eingebaut)
# - threading (Threading - eingebaut)
# - datetime (Datum/Zeit - eingebaut)
# - os, sys, pathlib (System - eingebaut)
# - logging (Logging - eingebaut)
# - subprocess (Prozesse - eingebaut)
# - urllib (URL-Verarbeitung - eingebaut)
#
# ========================================
